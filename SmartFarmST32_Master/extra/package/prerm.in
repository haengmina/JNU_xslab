#!/bin/bash

source /etc/os-release
case "$ID" in
	"ubuntu"|"debian")
		if [ "$1" == "upgrade" ]; then
			#Do Not Run when Upgrade (dpkg -i)
			exit 0
		fi
		;;

	"rocky")
		if [ "$1" != "0" ]; then
			#Do Not Run when Upgrade (rpm -U)
			exit 0
		fi
		;;
esac
